/**
* _isdigit - checks for a digit
*@c: a variable that stored an interger number
*
* Return: 1 for interger and 0 for others
*/
int _isdigit(int c)
{

if (c >= 48 && c <= 57)
{

return (1);

}

return (0);
}
