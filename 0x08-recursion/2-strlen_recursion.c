/**
*_strlen_recursion - count number of letters
*@s: a pointer variable
*/
int _strlen_recursion(char *s)
{

int str_len=0;

if (*s != '\0')
{
count = count +1;

_stren_recursion(s + 1);
}

return (str_len);
}
